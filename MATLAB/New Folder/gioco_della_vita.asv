clear,clc

N = 30;
A = zeros([N,N]);
A([304,305,306]) = 1;

numgen = 100;
C = conv2(A,[1 1 1;1 0 1;1 1 1], 'same');

for ii = 1:100
    B = zeros([N,N]);
    B(C(A==1)>2) = 1;
    B(C>3) = 1;
    spy(B)
    drawnow
    A = B;
end
